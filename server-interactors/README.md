# `@angle-measurer/server-interactors`

Contains interfaces and services for connecting to a browser server, for the purpose of running suites across multiple browsers.

Concretions will adhere to whichever tool/protocol is being used, up to and including a simple proxy-pass to a direct connection of a single instance of Chrome.